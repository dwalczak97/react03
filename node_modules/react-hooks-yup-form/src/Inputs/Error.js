import React from "react";
import PropTypes from "prop-types";

const Error = ({ touched, error }) => {
  return <>{touched && !!error && <div className="error">{error}</div>}</>;
};

Error.propTypes = {
  touched: PropTypes.bool,
  error: PropTypes.string
};

export default Error;
